# Route-B Draft v2 Open Issues

## Blocking before submission, not before the next GPT review

1. Resolve the 23 `MANUAL_OFFICIAL_CHECK_REQUIRED` bibliography entries listed in `BIBLIOGRAPHY_AUDIT.md`, then make a separate, audited bibliography-only update.
2. Obtain external review of whether the route-ledger framework and Algorithm 1 are sufficiently constructive for the intended KBS reliability/audit positioning.
3. Confirm author names, affiliations, and corresponding-author details.
4. Decide whether the current 10-page independent draft should be expanded with a short formal implementation/availability statement after scientific approval.

## Frozen boundaries

- Do not run new training, start Stage 1b, inspect seeds 4-8 model-training or model-performance outputs, or use GPU.
- Do not replace `E:\GUOJI\elsarticle\istf_kbs.tex`.
- Do not reintroduce historical root-cause synthetic, benchmark-performance, operating-regime, or legacy ISTF-Mamba performance narratives.
- Do not change the meaning of the Stage 1a negative result or the P1 `INCONCLUSIVE_BOUNDED_POSTMORTEM` classification.

## Non-blocking presentation checks for review

- Confirm whether the main text should retain all five audit labels or reserve `UNASSESSED` for the appendix/template.
- Confirm whether Table 1 should remain a full route-ledger example table or be shortened after the framework’s novelty is reviewed.
- Confirm whether the five-seed P0 bar panel should remain in the main text or move to the appendix if page budget tightens.

