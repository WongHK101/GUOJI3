# Phase 8 Implementation CPU Preflight v1

This review package demonstrates implementation and CPU semantic readiness
only. It contains no scientific result, checkpoint, manuscript, or GPU output.

The primary direct-Granger object is total raw-chain attribution over the
declared nominal lag support. Reliable-support and unrestricted full-history
maxima are saved only as secondary/exploratory diagnostics.

The package must include:

- implementation source and base-to-release patch;
- CPU tests and complete test report;
- exact-reference, finite-difference/chain, deterministic-schedule,
  comparator-parity, matrix-validation, and provenance-field reports;
- source/config/matrix/schedule SHA256 manifests;
- commit information and reproduction commands;
- changelog and unresolved risks.

The package does not include a confirmation release token. GPU work remains
prohibited until GPT approves this implementation package.
