# Phase 8 Recovery Documentation

These files supersede the repair-estimator and GPU-preflight instructions in
`docs/phase8_execution/` for the recovery branch only. The older directory is
retained unchanged as provenance for the frozen stopped-preflight release.

The scientific objective, model, lambda, horizon, scores, run matrix, seeds,
and pilot thresholds are unchanged. Only the repair Monte Carlo sampler and
its preflight reachability diagnostics are revised. Confirmation remains
sealed.

Use the documents in this directory together with the commit-bound release
manifest. Do not combine the revised sampler with source from another commit.
